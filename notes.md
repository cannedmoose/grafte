Want view to be able to:

Update after another view (already possible)
Draw bounds of other views
draw/not draw selection/guides

options: {
drawSelected: bool;
drawBounds: [{bound: Rectangle, stroke: strokeStyle}]
}
